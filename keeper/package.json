{
  "name": "gbpf-forwarder-keeper",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that scans forwarder deposits and calls sweepAndExecute on the GBPF batchers.",
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.86.0"
  }
}
